
<% include header %>
			<!-- Navbar -->
<% if(transparent){ %>
<nav class="navbar navbar-transparent sticky-top navbar-expand-lg navbar-dark bg-dark" id="homenav">
	<% }else{ %>
	<nav class="navbar navbar-transparent sticky-top navbar-expand-lg navbar-dark bg-dark" id="homenav">
	<% } %>
	<div class="container">
		<div class="square" id="box">
			<a class="navbar-brand" href="/"><img src="images/aahvaan_logo.jpg"></a>
		</div>
	<button id="sandwich" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav-item">
				<% if(transparent){ %>
				<a class="nav-link" id="Events">Events</a>
				<% }else{ %>
				<a class="nav-link" id="Events" href="/">Events</a>
				<% } %>
			</li>
			 <li class="nav-item">
				<a class="nav-link" href="/campus_ambassador">Campus Ambassador</a>
			</li>
			 <li class="nav-item">
			 	<% if(transparent){ %>
				<a class="nav-link" id="Theme">Theme</a>
				<% }else{ %>
				<a class="nav-link" id="Theme" href="/">Theme</a>
				<% } %>
			</li>
			 <li class="nav-item">
			 	<% if(transparent){ %>
				<a class="nav-link" id="Lookback">Lookback</a>
				<% }else{ %>
				<a class="nav-link" id="Lookback" href="/">Lookback</a>
				<% } %>
			</li>

<!-- 			 <li class="nav-item">
				<a class="nav-link" href="#">Sponsors</a>
			</li> -->
			 <li class="nav-item">
			 	<% if(transparent){ %>
				<a class="nav-link" id="Live">Live</a>
				<% }else{ %>
				<a class="nav-link" id="Live" href="/">Live</a>
				<% } %>
			</li>
			</li>
<!-- 			 <li class="nav-item">
				<a class="nav-link" href="#">Reach Us</a>
			</li> -->
		</ul>
		<ul class="navbar-nav ml-auto">
		 	<% if(username){ %>
		 	<li class="nav-item reg-btn">
				<a href="/register_teams"><button class="btn btn-outline-warning btn-sm" type="button">Register Teams</button></a>
			</li>
			<li class="nav-item reg-btn">
				<a href="/logout"><button class="btn btn-sm btn-outline-warning" type="button" id="name_logout">Hello,<%= (username.substr(0,username.indexOf(' '))).substring(0,6); %></button></a>
			</li>

		 	<% } else { %>
		 	<li class="nav-item reg-btn">
				<a href="/register"><button class="btn btn-sm btn-outline-warning btn-sm" type="button">Register Leader</button></a>
			</li>
			<li class="nav-item reg-btn">
				<a href="/login"><button class="btn btn-sm btn-outline-warning" type="button">
				Log In</button></a>
			<% } %>
			</li>
		</ul>
	</div>
</div>
</nav>

